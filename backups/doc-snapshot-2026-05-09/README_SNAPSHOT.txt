Снимок документации и правил агента перед приведением в порядок (2026-05-09).

Содержимое:
- корневые *.md (API_AUDIT, PROJECT_AUDIT, CHANGELOG, README, SESSION_LOG);
- папка docs/ (копия);
- папка cursor-rules/ (копия .cursor/rules);
- папка Для_анализа/ (копия «Для анализа»).

Восстановление: скопировать нужные файлы из этой папки поверх рабочей копии проекта.
